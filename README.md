# web485
Another IOT application that provides a network interface to a device that has an RS485 bus interface.

The original intent was to provide generic register access but due to time constraints the implementation is specific to the TUF-2000B Ultrasonic Flow Meter.
